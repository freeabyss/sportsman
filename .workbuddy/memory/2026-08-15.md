# 2026-08-15

## 评分体系二次重构：从“积分排名”改为“纯奖牌数排名”

用户要求：去掉积分、统治力相关功能和说明，排名改为仅按三大赛奖牌数排。同层排序确认采用“金牌优先”（单打金>银>铜>总数，再双打，再团体）。

### 改动（commit b1e8226，已推 Vercel）
- `config.json`：删除 project_weights / medal_weights / tournament_weight / dominance_weights，仅留 major_tournaments、evaluation_target、rule_version、category_order、category_codes。
- `scoring.js`：重写为 `calcMedalStats(athleteId, events, tournaments)`，返回 medals / categories(singles/doubles/team 各 gold/silver/bronze/total) / achievements。删除所有算分函数。
- `dataService.js`：getAllAthleteStats 改调 calcMedalStats；getRankings 排序改为 单打>双打>团体、金>银>铜>总数（纯奖牌，无积分）；删除 getAthleteContributions，新增 getAthleteAchievements；移除外层 stats 导入（calcWinRates/getTopRivals 仍由 AthletePage 直接调用，用于“主要对手”）。
- `HomePage.vue`：移除积分公式卡（含统治力），改简洁“排名规则”卡；TOP3 去积分/统治力，改显奖牌总数+单打/双打/团体金牌；排名表删积分列与统治力列，新增 单打/双打/团体(金/总) 与 金牌合计 列。
- `AthletePage.vue`：荣誉概览删积分卡、统治力卡，改 单打金/双打金/团体金/奖牌统计；三大赛成绩记录表删“得分”列，改用 achievements；onMounted 改用 getAthleteAchievements；删除 formatScore。
- 删除 `AthleteScorePage.vue` 及其路由 `/athlete/:id/score`。
- `stats.js`：删除 calcDominanceIndex、rankAthletes、未用的 config 导入。
- `App.vue` footer “排名公式”→“排名规则”；`README.md` 重写排名规则章节，移除积分分析/积分矩阵/统治力说明。
- 构建通过；dist 旧产物含 AthleteScorePage 等残留 JS，下次 Vercel 部署会覆盖。

### 验证结果（新规则 TOP）
张怡宁1 / 王楠2 / 马龙3 / 樊振东4 / 丁宁5 … 王皓11、樊振东4。单打金牌优先后樊振东(7单打金)明显高于王皓(4单打金)，符合用户预期。
